import React from 'react';
import PropTypes from 'prop-types';

const SocialMediaBtn = ({ handle, link, domain }) => {
  return (
    <div>

    </div>
  );
};

SocialMediaBtn.propTypes = {

};

export default SocialMediaBtn;